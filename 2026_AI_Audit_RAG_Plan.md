# 稽核型複合問答規劃書(草案)

> 版本 v0.1 · 2026-09-10 · 狀態:規劃中,尚未實作
> 前提:PR #4 已上線的 RAG 問答(9 個單集合統計工具 + 文件檢索)。本文規劃**下一階段**:以稽核視角的複合型問題。

## 一、背景與目標

後續訪談結論:主要使用者以**稽核角度**使用 RAG,問題是跨資料集合、需要「判定」而非「統計」的複合題。代表問題:

| 代表問題 | 本質 |
|---|---|
| 今日施工的工人中,因需高空作業而應具備高空作業證照者,是否都有證照? | 出工名單 × 當日作業類別 × 應具證照 × 持有/到期 → **符合/不符合** |
| 這個工人在其他工地有過幾次違規? | 單一工人的**跨工地**違規歷史(工地隔離的例外) |

目標:讓 AI 對這類問題回覆「**判定 + 逐人證據 + 缺漏項**」,數字與口徑可驗收,並附可點擊的來源(工人卡、許可單、缺失單)。

## 二、現況缺口(依實際 schema 盤點)

現有工具皆「單集合、回統計數字」;稽核題需要的 join 鏈與資料事實:

| 資料 | 位置 | 可用性 |
|---|---|---|
| 今日出工者 | `siteForm(toolboxMeeting, applyDate=今日).healthWarnings.*Signatures[]` 含 `name / company / idno` | ✅ `idno` 可精確 join 到 `worker.idno`(同名不會混淆) |
| 當日作業類別 | `siteForm(sitePermit 涵蓋今日).selectedCategories[]` + `contractor` | ⚠️ 許可單**沒有作業人員名單**,只有承攬商與人數 |
| 工人證照 | `worker.certifications[{type, name, issue, withdraw}]`,18 種 `CertificationType` | ✅ 有到期日 `withdraw` |
| 違規記錄 | `worker.safetyIssues[{siteId, formId, issueDate}]`(**全域集合,跨工地都在同一份工人文件**)+ `siteForm(safetyIssueRecord)` 明細 | ✅ 次數/日期可直接答;⚠️ 類別需二次查 siteForm |

**四個關鍵缺口(P0 決策項,需帆宣確認)**

1. **作業類別 → 應具證照對照表不存在。** 這是業務規則,系統裡沒有。草案(待確認):

   | 許可單作業類別 | 應具證照(CertificationType) | 備註 |
   |---|---|---|
   | 高架作業 | A 高空作業車操作人員 | 僅使用高空作業車時?或所有高架作業? |
   | 局限空間作業 | O2 缺氧(局限)作業主管 | 法規要求現場有作業主管(至少 1 人)而非人人持證 |
   | 施工架組裝作業 | SA 施工架組配作業主管 | 同上,主管制 |
   | 動火作業 | OW 氧乙炔熔接裝置作業人員 | 僅氧乙炔時 |
   | 起重吊掛作業 | (系統無對應證照類型) | 需新增類型或標「無法判定」 |
   | 電力/吊籠/管線拆離/開口/化學 | 待定 | |

   注意「**人人持證**」與「**現場至少一名作業主管**」是兩種不同判定規則,對照表要標明。

2. **許可單無人員名單,「誰在做高空作業」只能近似。** 選項:(a) 近似法——同承攬商當日出工者 ∩ 該承攬商當日特殊作業許可單,回答時明示「依承攬商推定」;(b) 許可單表單擴充「作業人員」欄位(表單改版,資料自此才準)。建議 (a) 先上、(b) 列入表單改版。

3. **跨工地查詢的隔離原則**(目前系統承諾 A 工地查不到 B 工地)。草案規則:
   - 只允許查「**目前工地在冊**」(`worker.belongSites` 含本站)的工人
   - 他站違規只回**次數與日期**,不回他站缺失單內容、照片、責任單位
   - 每次跨站查詢留痕(對應產品規格書 九(二)3「權限判定紀錄留存」)

4. **`safetyIssues` 只存 siteId/formId/issueDate,沒有類別。** 「違規幾次」可答;「違規哪類」要回查各站 `siteForm`——他站表單依第 3 點不揭露,故他站只能給次數。

## 三、方案比較

| 方案 | 做法 | 優 | 劣 |
|---|---|---|---|
| A. 細粒度工具 + LLM 自己串 | 加「查出工名單」「查證照」「查許可單」等工具,靠多輪 tool loop 組合 | 彈性高 | 口徑不可控、每輪 ~20s 首字、判定邏輯在 LLM 手上 → **不適合稽核驗收** |
| B. 稽核專用工具 | server 端做 join 與判定,回結構化 verdict | 可驗收、可測、快(1 輪) | 每種稽核題要寫一支 |
| **C. 混合(推薦)** | 少數稽核工具負責「判定」,既有細粒度工具負責追問 | 兼顧 | — |

## 四、稽核工具設計(第一批)

沿用 `server/routes/aiTools.js` 的 `{content, links}` 契約,siteId 由後端綁定。

| 工具 | 參數 | 回傳(給 LLM) | 回答的問題 |
|---|---|---|---|
| `audit_worker_qualifications` | `date?`(預設今日)、`category?`(預設當日全部特殊作業) | 每人 `{姓名, 公司, 作業類別, 應具證照, 持有證照, 到期日, 判定}` + 總結 `{符合 n, 不符合 n, 無法判定 n}` + 推定依據說明 | 今日高空作業的人都有證照嗎? |
| `get_worker_profile` | `name` 或 `idno` | 基本資料、公司、證照清單(含到期狀態)、本站違規次數、他站違規次數(依隔離規則)、是否在冊 | 這個工人的狀況? |
| `get_worker_violations` | `name`/`idno`、`scope: this_site \| all_sites` | 本站:次數 + 日期 + 缺失代碼/類別(附缺失單連結);他站:僅次數 + 日期 + 工地數 | 他在其他工地違規幾次? |
| `audit_expiring_certifications` | `days`(預設 30) | 在冊工人證照到期/已過期清單 | 誰的證照快到期? |
| `audit_permit_coverage` | `date?` | 當日出工承攬商是否都有有效許可單;有出工無許可單者列出 | 今天有沒有人沒許可單就進場? |

第二批(對應產品規格書 B/F 群組):`audit_hazard_notice_completion`(危害告知簽名完成度)、`audit_training_completion`(教育訓練完訓率)、`audit_insurance_status`(勞保/意外險缺漏)。

**判定輸出規範**:`verdict` 只有三值「符合 / 不符合 / 無法判定(資料不足)」;證據逐筆列出;LLM 只**轉述**工具判定,不得自行推論。system prompt 加一條:「稽核類問題以工具的判定為準,無法判定時明講缺什麼資料」。

**連結**:沿用 links 機制——工人卡 `/site/{siteId}/workerList/{workerId}`(路由待確認)、許可單、缺失單。

## 五、隔離與稽核留痕

- 新增 `ai_tool_audit` 集合:`{siteId, userId, sessionId, tool, args, crossSite: bool, at}`;跨站查詢必記
- 跨站工具在回傳裡明示「依隔離規則,他站僅提供次數」

## 六、分期與工作量估算

| 階段 | 內容 | 估計 |
|---|---|---|
| **P0 決策(帆宣)** | 作業類別→證照對照表(含人人持證 vs 主管制)、許可單是否加人員欄位、跨站違規揭露範圍 | 訪談 1 次 |
| **P1** | `audit_worker_qualifications`、`get_worker_profile`、`get_worker_violations`、隔離規則、留痕、system prompt 調整、單元測試 | ~3 人日 |
| **P2** | `audit_expiring_certifications`、`audit_permit_coverage`、第二批三支 | ~2 人日 |
| **P3** | 前端稽核結果以表格 + 判定 chip 呈現(取代純文字)、匯出 | ~2 人日 |

## 七、風險

- **身分比對**:簽到有 `idno` 時精確;缺 `idno` 降級「姓名 + 公司」並在判定標「身分未確認」
- **資料品質**:證照 `withdraw` 空值、許可單 `selectedCategories` 含工地自訂文字(對照表要容忍未知類別 → 無法判定)
- **口徑漂移**:稽核工具的判定規則需與帆宣書面確認並寫進 CLAUDE_MODULES.md,避免與日後 Dashboard 新卡片不一致
- **與產品規格書對齊**:本文工具需對應附錄一的 T 編號(F 群組「人員與資格」6 項),避免兩套清單
